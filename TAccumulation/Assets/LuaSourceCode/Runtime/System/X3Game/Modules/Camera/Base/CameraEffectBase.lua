---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by chaoguan.
--- DateTime: 2021/4/12 11:18
---

---镜头效果基类，
---由VirtualCamera类持有，负责镜头效果项的需求，如后处理、震屏等等
---@class CameraEffectBase:CameraBase
---@field virtualCamera VirtualCameraBase
local CameraEffectBase = class("CameraEffectBase", require("Runtime.System.X3Game.Modules.Camera.Base.CameraBase"))

---@param virtualCamera VirtualCameraBase
function CameraEffectBase:ctor(virtualCamera)
    self.virtualCamera = virtualCamera
end

function CameraEffectBase:Play(name, priority, ...)
    self.name = name
    self.priority = priority
end

function CameraEffectBase:Stop(name)
    if self.name ~= name then
        return
    end
end

return CameraEffectBase