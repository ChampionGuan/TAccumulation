---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by pc.
--- DateTime: 2020/11/24 19:57
---

local CatCardConst = require("Runtime.System.X3Game.Modules.CatCard.Data.CatCardConst")
local CatCardBaseCtrl = require(CatCardConst.BASE_CTRL_PATH)
local CatCardEffectCtrl = class("CatCardEffectCtrl",CatCardBaseCtrl)

function CatCardEffectCtrl:GetTypeMap()
    return CatCardConst.EffectType
end

function CatCardEffectCtrl:GetHandler(effect_type)
    local ctrl = self.node_map[effect_type]
    if not ctrl then
        ctrl = require(CatCardConst.EffectConf[effect_type]).new()
        ctrl:SetBll(self.bll)
        ctrl:SetOwner(self)
        self.node_map[effect_type] = ctrl
    end
    return ctrl
end

function CatCardEffectCtrl:Check(effect_type,...)
    local ctrl = self:GetHandler(effect_type)
    if ctrl then
        ctrl:Execute(...)
    end
end


function CatCardEffectCtrl:Enter()
    self.super.Enter(self)
end

function CatCardEffectCtrl:Exit()
    self.super.Exit(self)
end

return CatCardEffectCtrl