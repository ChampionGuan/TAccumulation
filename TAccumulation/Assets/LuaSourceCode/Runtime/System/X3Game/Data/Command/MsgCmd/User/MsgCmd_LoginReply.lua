--- Generated by AutoGen
---
---@class MsgCmd_LoginReply
local MsgCmd_LoginReply = class("MsgCmd_LoginReply", require("Runtime.System.Framework.GameBase.Misc.SimpleCommand"))

---@param data pbcmessage.LoginReply
function MsgCmd_LoginReply:Execute(data)
    GrpcMgr.OnLoginReply(data)
    if UNITY_EDITOR then
        ---只做服务器proto版本提示 不使用 messagebox 改为 Log 提示
        local pbcVersion = GrpcMgr.GetProtoVersion()
        local showMessage = false
        if pbcVersion ~= 0 and data.ProtoVersion ~= 0 then
            showMessage = pbcVersion ~= data.ProtoVersion
        end
        if showMessage then
            Debug.LogError("---【Editor】 当前客户端proto版本和服务器不一致，如果发生阻断问题，请尝试 1.更新客户端 2.检查私服是否重新构建 3.联系服务器重启")
        end
    end

end

return MsgCmd_LoginReply
