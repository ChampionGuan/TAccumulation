---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by jiaozhu.
--- DateTime: 2021/12/23 16:38
---Joystick组件

local cs_ui_util = CS.X3Game.UIUtility

---添加Joystick的onJoystickDown&onJoystickUp回调
---支持X3Joystick
---@param onJoystickDown fun(type:UnityEngine.EventSystems.PointerEventData)
---@param onJoystickUp fun(type:UnityEngine.EventSystems.PointerEventData)
function UIUtil.AddJoystickDownUpListener(obj, onJoystickDown, onJoystickUp)
    if obj then
        cs_ui_util.AddJoystickDownUpListener(obj, onJoystickDown, onJoystickUp)
    end
end

---添加Joystick的onJoystickDrag回调
---支持X3Joystick
---@param onJoystickDrag fun(type:Vector2)
function UIUtil.AddJoystickDragListener(obj, onJoystickDrag)
    if obj then
        cs_ui_util.AddJoystickDragListener(obj, onJoystickDrag)
    end
end

---添加Joystick的onJoystickUpdate回调
---支持X3Joystick
---@param onJoystickUpdate fun(type:Vector2)
function UIUtil.AddJoystickUpdateListener(obj, onJoystickUpdate)
    if obj then
        cs_ui_util.AddJoystickUpdateListener(obj, onJoystickUpdate)
    end
end

---添加Joystick的onJoystickLateUpdate回调
---支持X3Joystick
---@param onJoystickLateUpdate fun(type:Vector2)
function UIUtil.AddJoystickLateUpdateListener(obj, onJoystickLateUpdate)
    if obj then
        cs_ui_util.AddJoystickLateUpdateListener(obj, onJoystickLateUpdate)
    end
end

---添加Joystick的onJoystickFixUpdate回调
---支持X3Joystick
---@param onJoystickFixUpdate fun(type:Vector2)
function UIUtil.AddJoystickFixUpdateListener(obj, onJoystickFixUpdate)
    if obj then
        cs_ui_util.AddJoystickFixUpdateListener(obj, onJoystickFixUpdate)
    end
end

---添加Joystick的onEnterDeadArea&onExitDeadArea回调
---支持X3Joystick
---@param onEnterDeadArea fun()
---@param onExitDeadArea fun()
function UIUtil.AddJoystickDeadAreaListener(obj, onEnterDeadArea, onExitDeadArea)
    if obj then
        cs_ui_util.AddJoystickDeadAreaListener(obj, onEnterDeadArea, onExitDeadArea)
    end
end

---添加Joystick的onEnterWalkArea&onExitWalkArea回调
---支持X3Joystick
---@param onEnterWalkArea fun()
---@param onExitWalkArea fun()
function UIUtil.AddJoystickWalkAreaListener(obj, onEnterWalkArea, onExitWalkArea)
    if obj then
        cs_ui_util.AddJoystickWalkAreaListener(obj, onEnterWalkArea, onExitWalkArea)
    end
end

---清理回调
---@param obj UObject
function UIUtil.RemoveJoystickListener(obj)
    if obj then
        cs_ui_util.RemoveJoystickListener(obj)
    end
end