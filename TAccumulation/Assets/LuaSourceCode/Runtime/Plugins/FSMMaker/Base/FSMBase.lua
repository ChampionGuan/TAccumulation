---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by jiaozhu.
--- DateTime: 2023/8/8 18:02
---所有FSM体系内的最基础类
---@class FSM.FSMBase
---@field fsm FSM.FSM 状态机
---@field fsmState FSM.FSMState 所属状态
---@field context FSM.FSMContext context
---@field owner FSM.FSMBase
---@field id int 唯一id
---@field name string
---@field private ownerId int 所属ownerId
---@field private componentType FSM.FSMComponent
---@field private disabled bool 是否禁用
local FSMBase = class("FSMBase")

--region 周期函数

---@private
function FSMBase:OnAwake()

end

---@private
function FSMBase:OnReset()

end
---@private
function FSMBase:OnDestroy()

end
--endregion

--region 操作函数，底层调用

--region 对外操作函数
---@private
---@param isEnabled boolean
function FSMBase:SetEnabled(isEnabled)
    self.disabled = not isEnabled
end

---@private
---@return boolean
function FSMBase:IsEnabled()
    return not self.disabled
end

---@private
---@param name string
function FSMBase:SetName(name)
    self.name = name
end

---@private
---@param owner FSM.FSMBase
function FSMBase:SetOwner(owner)
    self.owner = owner
end

---@private
---@param id int
function FSMBase:SetId(id)
    self.id = id
end

--endregion

---@private
---@param fsm FSM.FSM
---@param fsmState FSM.FSMState
---@param context FSM.FSMContext
---@param id int
---@param owner FSM.FSMBase
function FSMBase:SetBasic(name, fsm, fsmState, context, id, owner)
    self.fsm = fsm
    self.fsmState = fsmState
    self.context = context
    self.id = id
    self.owner = owner
    self.name = name
end

---@private
---@param compType FSM.FSMComponent
function FSMBase:SetComponentType(compType)
    self.componentType = compType
end

---@private
---@return FSM.FSMComponent
function FSMBase:GetComponentType()
    return self.componentType
end

---@private
---设置所属ownerId
---@param id int
function FSMBase:SetOwnerId(id)
    self.ownerId = id
end

---重置
---@private
function FSMBase:Reset()
    self:OnReset()
end

---清理
---@private
function FSMBase:Destroy()
    self:OnDestroy()
    table.clear(self)
end

function FSMBase:ToString()
    return string.format("id=[%s],name=[%s]", self.id, self.name)
end
--endregion

return FSMBase