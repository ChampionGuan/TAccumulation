---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by canglan.
--- DateTime: 2021/11/18 11:04
---

local BubbleBase = require("Runtime.Battle.Behavior.UI.Bubble.BubbleBase")

---@class MaleBubble:BubbleBase
local MaleBubble = XECS.class("MaleBubble", BubbleBase)
MaleBubble.Type = BattleBubbleBehaviorType.MaleBubble
local csBattleUtil = CS.X3Battle.BattleUtil
local csDummyType = CS.X3Battle.DummyType

function MaleBubble:Awake()
    self._messageName = "OCX_boy_message"
    self._bubbleName = "OCX_boy_bubble"
    self._contentName = "OCX_boy_txt_content"
    BubbleBase.Awake(self)
end

function MaleBubble:_OnUpdate()
    if self._battleUI.boy and not self._actor then
        self._actor = self._battleUI.boy
        self._actorTrans = self._actor:GetDummy()
        self._dialogTrans = csBattleUtil.GetActorDummy(self._actor, csDummyType.PointDialog)
    end
    BubbleBase._OnUpdate(self)
end

return MaleBubble